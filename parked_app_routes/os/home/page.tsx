"use client";
import SectorTemplate from "../_sector-template";
export default function Page() {
  return (
    <SectorTemplate
      title="OS Home"
      subtitle="Main deck: system status + quick actions."
      hint="Use this as your cockpit landing area: quick launch, recent missions, and system status."
    />
  );
}
